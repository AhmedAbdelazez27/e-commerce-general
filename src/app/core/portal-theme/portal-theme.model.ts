/** Theme tokens applied to CSS variables — no business/UI flags. */
export interface PortalThemeTokens {
  primaryColor: string;
  secondaryColor: string;
  accentColor: string;
  successColor: string;
  dangerColor: string;
  warningColor: string;
  backgroundColor: string;
  textColor: string;
  headerColor: string;
  footerColor: string;
  fontFamilyAr: string;
  fontFamilyEn: string;
  fontSizeBase: number;
  fontSizeHeading: number;
  fontSizeSmall: number;
  borderRadius: string;
  faviconUrl: string;
}
